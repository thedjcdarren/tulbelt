// Tulbelt dev probe — PERMANENT NO-OP STUB. Contract (docs/devtools.md):
// coding agents temporarily replace the body with throwaway capture code that
// uses window.__tulbelt helpers (log/snapshot/tree/watch), ask the user to
// reload the extension + tab, reproduce, and run __tulbelt.copy() in the
// DevTools console (Tulbelt context). RESTORE THIS FILE TO THIS EXACT STUB
// BEFORE COMMITTING. It must remain safe and side-effect-free when untouched.
// Runs in the isolated world, top frame, document_idle, after all toggles.

(() => {
  // Intentionally empty.
})();
