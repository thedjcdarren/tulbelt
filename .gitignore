# Local Claude Code settings (machine-specific permission allowlists)
.claude/settings.local.json

# Dependencies (none are committed; only used for ad-hoc tooling via npx)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
